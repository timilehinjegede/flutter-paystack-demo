import 'package:flutter/material.dart';

class SelectCheckoutOptionScreen extends StatelessWidget {

  static final String routeName = 'selectoptioncheckoutscreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(
            'Select Option Checkout Screen'
        ),
      ),
      body: Center(
        child: Text(
            'Select Option Checkout Screen'
        ),
      ),
    );
  }
}
